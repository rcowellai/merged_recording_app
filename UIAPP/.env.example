# ============================================================================
# UIAPP Firebase Environment Configuration (C02)
# ============================================================================
#
# DEVELOPER HANDOFF NOTES:
# - Converted from MVPAPP environment variables (VITE_ → REACT_APP_)
# - Copy this file to .env.local and fill in actual values
# - All Firebase config variables are REQUIRED for Firebase functionality
# - Feature flags allow gradual enablement of Firebase services
#
# MVPAPP SOURCE: recording-app/.env.example
# UIAPP TARGET: .env.example
# CONVERSION: VITE_ → REACT_APP_ prefix change
#
# ============================================================================

# Firebase Configuration (REQUIRED for Firebase functionality)
# Get these values from Firebase Console → Project Settings → General
REACT_APP_FIREBASE_API_KEY=your_api_key_here
REACT_APP_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=your_project_id
REACT_APP_FIREBASE_STORAGE_BUCKET=your_project.firebasestorage.app
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=123456789
REACT_APP_FIREBASE_APP_ID=1:123456789:web:abcdef123456
REACT_APP_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# Firebase Feature Flags (REQUIRED)
# Control which Firebase services are enabled
REACT_APP_USE_FIREBASE=false
REACT_APP_FIREBASE_AUTH_ENABLED=false
REACT_APP_FIREBASE_STORAGE_ENABLED=false
REACT_APP_SESSION_VALIDATION_ENABLED=false

# Application Environment (REQUIRED)
# Controls debugging, analytics, and feature availability
REACT_APP_ENVIRONMENT=development
REACT_APP_DEBUG_MODE=true

# Storage Configuration (OPTIONAL)
# Controls which storage backend to use
REACT_APP_STORAGE_TYPE=local

# Firebase Emulator Configuration (DEVELOPMENT ONLY)
# Enable local Firebase emulators for development
REACT_APP_USE_EMULATOR=false
REACT_APP_EMULATOR_AUTH_URL=http://localhost:9099
REACT_APP_EMULATOR_FIRESTORE_URL=http://localhost:8080
REACT_APP_EMULATOR_FUNCTIONS_URL=http://localhost:5001
REACT_APP_EMULATOR_STORAGE_URL=http://localhost:9199

# ============================================================================
# Environment Setup Instructions
# ============================================================================
#
# 1. DEVELOPMENT SETUP:
#    - Copy this file to .env.local
#    - Set REACT_APP_USE_FIREBASE=false to use localStorage
#    - Set REACT_APP_DEBUG_MODE=true for detailed logging
#
# 2. FIREBASE DEVELOPMENT:
#    - Set REACT_APP_USE_FIREBASE=true
#    - Fill in Firebase config values from Firebase Console
#    - Set REACT_APP_USE_EMULATOR=true for local development
#    - Start Firebase emulators: npm run emulate
#
# 3. PRODUCTION SETUP:
#    - Set REACT_APP_ENVIRONMENT=production
#    - Set REACT_APP_DEBUG_MODE=false
#    - Set REACT_APP_USE_FIREBASE=true
#    - Set all Firebase feature flags to true
#    - Set REACT_APP_USE_EMULATOR=false
#
# Sentry Error Tracking (OPTIONAL - Production Recommended)
# Sign up: https://sentry.io/signup/
# Get DSN from: Project Settings → Client Keys (DSN)
REACT_APP_SENTRY_DSN=
REACT_APP_SENTRY_ENVIRONMENT=development
REACT_APP_SENTRY_TRACES_SAMPLE_RATE=0.1

# ============================================================================
# Security Notes
# ============================================================================
#
# - Never commit actual environment values to repository
# - Firebase API keys are safe to expose in client-side code
# - Use Firebase Security Rules to control data access
# - Validate all environment variables are set before deployment
# - Sentry DSN is safe to expose (it's client-side only)
#
# ============================================================================